pipeline{
    agent any
    paremeters{
        booleanParam(defaultValue: false, description: "Enable Service", name: "myBoolean")
    }
    stages{
        stage("Demo"){
            steps{
                script{
                    if(params.myBoolean==false){
                        currentBuild.result="Success"
                        return
                    }else{
                        echo "The boolean is set to true"

                    }
                }

                

            }
           
        }
    }
}
